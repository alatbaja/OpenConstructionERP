version: '3.8'

services:
  app:
    image: python:3.12-slim
    working_dir: /opt/app
    volumes:
      - .:/opt/app
    command: python -m algeria_btph
    ports:
      - "8000:8000"
    environment:
      - BTPH_HOST=0.0.0.0
      - BTPH_PORT=8000
