"""هيكل مشروع الجزائر BTPH — دليل سريع."""

الموصلات الأساسية:
- `algeria_btph/`
  - `backend/`
    - `schemas/` — نماذج Pydantic (DTo/Validation)
    - `models/` — نماذج قاعدة البيانات لاحقًا
    - `services/` — دوال الأعمال (رفع ملفات، DPT/Décompte)
    - `storage/` — مسار ملفات الوثائق/الفواتير
  - `README.md`
